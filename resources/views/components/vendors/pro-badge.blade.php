{{-- A paid plan, drawn apart from the earned tier badges (gold Recommended,
     white Top) so nobody mistakes one for the other. --}}
<span {{ $attributes->class(['inline-flex shrink-0 items-center rounded-md bg-ink px-1.5 py-0.5 text-[10px] leading-none font-bold tracking-wider text-surface uppercase']) }} title="{{ __('marketplace.card.pro_title') }}">{{ __('marketplace.card.pro') }}</span>
